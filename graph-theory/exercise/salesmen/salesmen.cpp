#include "salesmen.h"

#include <string>
#include <vector>

using namespace std;

Database::Database() {
};

Database::~Database() {
};

bool Database::existsCustomer(string name) {
};

bool Database::existsSalesman(string name) {
};

Database::add (string salesman, string customer) {
};

vector<string> Database::findCustomers(string salesmen) {
};

vector<string> Database::findSalesman(string customer) {
};
